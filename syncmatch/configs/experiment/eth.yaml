# @package _global_
experiment:
  group: ETH_
  name: ${experiment.group}_${dataset.name}_nViews${dataset.num_views}_vSpace${dataset.view_spacing}
  timestamp: ""
dataset:
  name: ETH_Video
  num_views: 4
  view_spacing: 5
train:
  eval_step: 5000
  max_steps: 100000
